frame_component_durations
frame_id
frame_start_moments: VecDeque[2]

channel 1
1:1
main thread:async logic

channel 2
1:1
async logic:done checker

channel 3
1:1
done checker:main thread

A (0)
perform all necessary actions to be able to render in B

()
| start async done checking thread
->  | every cycle:
    | check if gpu done
    |   if so send duration (channel 3.1)
    | check if cpu done (channel 2)
    |   if so send duration (channel 3.2)
|
| start async logic thread
->  | block until needed (channel 1)
    | async cpu logic
    | send done to checker (channel 2)

B (1)
|
|
|

C (2)
| receive and set frame render duration (channel 3.1)
| receive and set frame logic duration (channel 3.2)
|
| if not both durations set
|   return
|
| if min time not elapsed
|   return
|
| update frame_start_moments
|
| display frame_component_durations
| reset frame_component_durations
|
| set frame_id to C
|
| frame prep
|
| set async cpu start time
| send start to async thread (channel 1)
|
| render cpu
| set render gpu start
->  | render gpu
    | signal fence
|
| ui
|
| base cpu logic
|
|
